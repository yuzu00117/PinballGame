//------------------------------------------------------------------------------
// Result
//------------------------------------------------------------------------------
// 役割:
// リザルト画面を表示するためのシーン用 GameObject。
// 描画（テキスト/背景など）を行い、必要に応じて入力を監視して遷移させる。
//
// 設計意図:
// Result は「シーン上の表示担当」として GameObject のライフサイクルに乗せる。
// 表示物（テキストや背景）を描画し、入力や状態に応じて上位のシーン管理へ通知する想定。
// 現状はテキスト描画のみを実装している。
//
// 構成:
// - ライフサイクル         : Init / Uninit / Update / Draw
// - 描画                   : Renderer::DrawText を使用（現状）
// - 描画用リソース          : 将来的に頂点/シェーダ/テクスチャを使う想定（メンバに保持）
//------------------------------------------------------------------------------
#pragma once

#include "gameobject.h"

/// リザルト画面を表示するシーン用オブジェクト
/// - Draw でリザルト表示を描画する
/// - Update で入力を監視し、必要に応じてシーン遷移する想定
class Result : public GameObject
{
public:
    // ----------------------------------------------------------------------
    // ライフサイクルメソッド
    // ----------------------------------------------------------------------
    /// 初期化処理
    /// - 必要な描画リソースを生成する（現状は未実装）
    void Init() override;

    /// 終了処理
    /// - 生成した描画リソースを解放する（現状は未実装）
    void Uninit() override;

    /// 更新処理（deltaTime は秒単位）
    /// - 入力監視や状態更新を行う（現状は未実装）
    void Update(float deltaTime) override;

    /// 描画処理
    /// - リザルト表示テキストを描画する
    void Draw() override;

private:
    // 将来的に描画用リソースを保持する場合はここにメンバを追加する
};
