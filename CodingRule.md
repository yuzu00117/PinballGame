## 命名規則
| 対象           | スタイル              | 例                                   |
| ------------ | ----------------- | ----------------------------------- |
| クラス / 構造体    | PascalCase        | `MeshRenderer`, `BoxCollider`       |
| enum の型名     | PascalCase        | `ColliderType`                      |
| enum の値      | PascalCase        | `ColliderType::Box`                 |
| 関数名          | PascalCase        | `Init()`, `CreateUnitBox()`         |
| ローカル変数       | lowerCamelCase    | `worldMatrix`, `vertexCount`        |
| 関数引数         | lowerCamelCase    | `int vertexCount`                   |
| メンバ変数        | `m_` + PascalCase | `m_VertexBuffer`, `m_Color`         |
| static メンバ変数 | `s_` + PascalCase | `s_ModelPool`                       |
| グローバル変数      | 原則禁止（必要なら `g_`）   | `g_Renderer`                        |
| 定数           | `k` + PascalCase  | `kMaxLights`, `kPi`                 |
| マクロ          | 全大文字 + アンダースコア    | `#define SAFE_RELEASE(p)`           |
| ファイル名        | PascalCase        | `MeshRenderer.h`, `BoxCollider.cpp` |


## アノテーションコメント規約

本プロジェクトでは、**アノテーションコメント**として
以下のタグのみを使用する。

* `TODO:`
* `FIXME:`
* `HACK:`
* `BUG:`
* `NOTE:`

### 使い分け指針

| 状態       | 使用タグ    |
| -------- | ------- |
| 将来対応予定   | `TODO`  |
| 問題があると認識 | `FIXME` |
| 意図的な暫定実装 | `HACK`  |
| 実害のある不具合 | `BUG`   |
| 仕様上の注意・設計メモ | `NOTE` |

# コメントスタイル コーディング規約

本ドキュメントは、本プロジェクトにおける **ソースコードコメントの統一基準** を定義する。

---

## 1. 基本方針

* コメントは「何をしているか」ではなく **設計意図・責務・契約** を記述する
* 実装詳細はコードで表現し、コメントは **変更に強い情報** に限定する
* 所有権・ライフサイクル・副作用は必ず明示する
* 書式を厳密に揃え、ファイル間での読解コストを下げる

---

## 2. ファイル先頭ヘッダコメント（必須）

### 2.1 書式

```h
//------------------------------------------------------------------------------
// <ファイル / クラス名>
//------------------------------------------------------------------------------
// 役割:
// 〜〜〜
//
// 設計意図:
// 〜〜〜
//
// 構成:
// - 〜〜〜
// - 〜〜〜
//
// NOTE:
// 〜〜〜
//------------------------------------------------------------------------------
```

### 2.2 ルール

* 罫線は必ず `//------------------------------------------------------------------------------`
* 見出し語彙は以下に固定

  * `役割:`
  * `設計意図:`
  * `構成:`
  * `NOTE:`
* 箇条書きは `// -` を使用
* NOTE には **制約・注意点・設計判断** を記述する

---

## 3. セクション区切りコメント（cpp側・必須）

### 3.1 書式

```cpp
// ------------------------------------------------------------------------------
// セクション名
// ------------------------------------------------------------------------------
// - 処理内容
// - 仕様・条件
// NOTE: 注意点
```

### 3.2 ルール

* 罫線は `// ------------------------------------------------------------------------------`
* セクションは **処理の意味的なまとまり** 単位で切る

  * 例：更新処理 / 描画処理 / 子オブジェクト管理 / 収集処理
* 箇条書きで「このブロックが何を保証するか」を示す

---

## 4. クラス・関数コメント（h側 / Doxygen風）

### 4.1 基本書式

```cpp
/// 要約（1行）
/// - 補足説明
/// - 契約・制約
```

### 4.2 必ず書くべき内容（該当する場合）

* 戻り値の性質

  * 所有 / 非所有
  * 有効期間
* 副作用

  * 内部で呼ばれる関数（Init 等）
* ライフサイクル上の注意

  * 遅延破棄 / 次回 Update 後に無効化 等

### 4.3 使用ルール

* `public` API は原則すべて `///` コメントを付与
* `private / protected` は設計上重要なもののみ記述
* 実装から自明な処理内容は書かない

---

## 5. 所有権・参照関係のコメント規約（メンバ変数）

### 5.1 書式例

```cpp
GameObject* m_Parent = nullptr;              // 非所有：親への参照
std::vector<std::unique_ptr<Component>> m_Components; // 所有：Component群
```

### 5.2 ルール

* ポインタ / smart pointer には必ず所有権を明記
* `unique_ptr` / `shared_ptr` の意図をコメントで補足
* フラグ変数には用途を短く明記

---

## 6. NOTE / TODO の使い分け

### NOTE

* 現時点での **設計判断・制約・注意点** を記述
* 利用者・保守者向けの情報

### TODO

* 将来的な改善・リファクタ予定
* 可能であれば理由や方針を併記

---

## 7. スタイル成立条件

### 成立条件

* ファイル先頭ヘッダが本規約の構造を満たしている
* cpp にセクション区切りが存在する
* public API に契約コメントが書かれている
* 所有権・ライフサイクルがコメントで明示されている

### 不成立条件

* 書式がファイルごとに異なる
* 戻り値ポインタの扱いがコメントから判断できない
* NOTE / TODO が理由なしで乱用されている

---

## 8. 適用範囲

* 本プロジェクトのすべての C++ ソースコード
* 新規ファイル作成時は本規約を必ず適用
* 既存コードは改修時に段階的に統一する
